prototype(Gerdemann.VRView:Hotspot) < prototype(Neos.Neos:Content) {
    scene = ${q(node).property('scene')}
    sceneIdentifier = ${q(this.scene).property('identifier')}
    sceneFilename = ${q(this.scene).property('asset').resource.filename}
}